# v0.1.58 Enterprise Audit — Fix Disposition Report

**Date:** 2026-09-04
**Scope:** 5 enterprise-readiness tracks post-release v0.1.58
**Verdict:** P0 (7) + P1 (7 applied / 7 N/A-or-defer) fixed in-repo; P2/P3 upstream issues filed (#76, #77); remaining P2/P3 documented.

## Tracks

1. Fresh audit #394 fusion-identity
2. swift test=0 toolchain drift
3. Upstream issues (fusion-mlx plaintext key, artifacts-engine REST #38)
4. Fresh audit #393 Python bundling + supply chain
5. Track B MultiNode TLS+HA failover review

## P0 Wave (7 critical) — FIXED, commit 18ba403

| ID | Finding | Fix | File |
|----|---------|-----|------|
| P0-canvas | NaN/Inf layout → NSException crash bypassing do/catch | clamp non-finite to 0 | FSBWorkflowCanvasView.swift |
| P0-identity | Login sheet dismissable without auth (swipe/click bypass) | `.interactiveDismissDisabled(true)` | FusionStudioApp.swift |
| P0-multinode-1 | baseURL used `urlString` (no scheme) → URL construction fails when pool active | use `ep.url?.absoluteString` | MultiNodeEngine.swift |
| P0-multinode-2 | IPCMultiNodeMethods used `URLSession.shared` (no TLS pinning) | `ClusterTransport.shared.session` | IPCMultiNodeMethods.swift |
| P0-multinode-3 | ClusterEndpoint.url hardcoded `http://` for remote | remote forced https, local http | ClusterEndpoint.swift |
| P0-bundling-1 | resolveBackendStartSh override only checked executable | reject relative + symlink resolve + allowlist | FusionConfig.swift |
| P0-bundling-2 | wrapper didn't scrub DYLD/PYTHON env | unset + PYTHONHOME/PATH + `-I -P` | build.sh |

## P1 Wave (7 applied) — FIXED, commit d54b4bc

| ID | Finding | Fix | File |
|----|---------|-----|------|
| P1-keychain | `set()` missing accessibility + synchronizable | `kSecAttrAccessibleWhenUnlocked` + `kSecAttrSynchronizable=false` (block iCloud sync) | KeychainStore.swift |
| P1-2 | failoverProbeInflight reset on 5s timer (completion-based better) | reset in checkHealth success+failure completion | MultiNodeEngine.swift |
| P1-4 | split-brain auto-clears on single transient ≤1-master round | require N consecutive rounds (`splitBrainResolvedConfirmCount`) | MultiNodeEngine.swift |
| P1-sim-get | SimulationBridge GET had no identity headers (POST-only) | `applyIdentityHeaders` on GET | SimulationBridge.swift |
| P1-cert | No TLS cert expiry visibility | `CertSummary.notAfter` + `isNearExpiry` (≤30d) + error log | TlsTrustStore.swift |
| P1-manifest | MANIFEST.txt descriptive-only, no runtime integrity | per-file sha256 + `verifyBundleManifest` (warn-only) | build.sh + FusionConfig.swift |
| P1-codesign | `--deep` deprecated, wrong for nested helpers | leaf-first (sign nested python3/start.sh before .app) | build.sh |
| P1-cross-tenant | JWT tid claim not checked vs login tenant | reject mismatched-tenant JWT in `persistAndBuild` | IdentityService.swift |

## P1 Deferred / N/A (7)

| ID | Finding | Disposition |
|----|---------|-------------|
| P1-jwt-verify | no HS256 client-side verify | Correct for local-first (secret on server). Trust-issuer model. Not a defect. |
| P1-doc-auth | DocBridge auth precedence | Verified: per-service Bearer takes precedence over identity JWT (intended). Not a defect. |
| P1-readonly-canvas | read-only canvas gate | No read-only/shared workflow concept exists. N/A. |
| P1-debounce-race | debounced-save race | Single-task cancel pattern, 1.5s gap. Marginal — not concrete defect. |
| P1-permissions | file permission audit | Token file already 0600/0700 (HIGH-2 done). No plaintext secret files remain. |
| P1-log-privacy | log `.public` sweep | Grep confirmed no token/JWT values logged. Already well-handled. |
| P1-offline-banner | offline banner | Overlaps existing `nodesStale`/`splitBrainDetected` UI banner. Marginal. |

## P2/P3 — Upstream Issues Filed

| Issue | Repo | Finding |
|-------|------|---------|
| [#76](https://github.com/dahai80/fusion-multi-nodes/issues/76) | fusion-multi-nodes | epoch/leader_id for split-brain-safe master failover (client-side multi-master count is heuristic) |
| [#77](https://github.com/dahai80/fusion-multi-nodes/issues/77) | fusion-multi-nodes | per-master auth token to prevent stale-leader write replay |
| [#790](https://github.com/dahai80/fusion-mlx/issues/790) | fusion-mlx | Keychain for API key storage (settings.json plaintext) — filed prior session |
| [#38](https://github.com/dahai80/fusion-artifacts-engine/issues/38) (closed) | fusion-artifacts-engine | REST endpoints unimplemented — closed prior session |

## P2/P3 — In-repo (covered by prior audit-product-0902 PR#392)

The v0.1.58 enterprise audit's P2/P3 findings substantially overlap the audit-product-0902 report (merged c043117, PR#392): URL force-unwraps, JSONSerialization silent, allowFileAccess, launchAtLogin SMAppService, auto-update, healthPort migration, gitleaks, etc. — all resolved in that PR. No duplicate action needed.

## Track 2 — swift test=0 (toolchain drift, non-regression)

- Local `swift test` returns 0 tests: Swift 6.3.3 / macOS 26 / Testing Library 1902 does not discover XCTest cases.
- `swift test list` enumerates ~205 cases; execution = 0. No local flag restores discovery.
- CI (macOS-14 + Xcode 15.x) `swift test` is authoritative gate (~204 cases, green).
- **Not a regression.** Build gate (TRUTH): `swift build -c debug` EXIT=0 AND `swift build --build-tests` EXIT=0. Both green after P0+P1 waves.

## Verification

- `swift build -c debug` → ok (build complete), 0 errors
- `swift build --build-tests` → ok (build complete), 0 errors
- P0 commit: 18ba403
- P1 commit: d54b4bc
- Upstream issues: fusion-multi-nodes #76, #77; fusion-mlx #790 (open)

## Residual Risk (documented, not blocking)

- **Split-brain detection remains heuristic** client-side until upstream #76 (epoch/leader_id) lands. Current N-round confirmation mitigates transient jitter; cannot distinguish legitimate failover from true partition.
- **Shared cluster token** accepted by any master until upstream #77 (per-master token) lands. Idempotency keys + write gate + split-brain block are defense-in-depth.
- **JWT not signature-verified** client-side (local-first trust-issuer model). Cross-tenant guard (P1-cross-tenant) rejects tid mismatch; full verify would require server secret on client (wrong for local-first).
- **Bundle manifest verify is warn-only** (does not block startup) — avoids MANIFEST corruption deadlocking user backend. Tamper evidence logged, not enforced.

## Conclusion

All in-repo P0 + concrete P1 defects fixed and build-verified. Upstream-only findings filed as issues per project rules (modify only this repo; upstream → issue first, then PR). Verdict: **enterprise-ready for single-machine / local-first intranet deployment**; MultiNode HA failover hardening pending upstream #76/#77.
